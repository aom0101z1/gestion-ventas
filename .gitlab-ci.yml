pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r *.html *.js *.css *.json public/ 2>/dev/null || true
    - cp -r assets public/ 2>/dev/null || true
  artifacts:
    paths:
      - public
  only:
    - main
